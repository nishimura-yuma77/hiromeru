--- a/docs/v.0.1/REQUIREMENTS.md
+++ b/docs/v.0.1/REQUIREMENTS.md
@@ -116,6 +116,7 @@
 - UIは承認時点のフォーム値そのものをAPI Request Bodyへ設定する
 - UIは現在の親Session IDをAPI Pathへ設定する
 - UIは承認操作ごとに`Idempotency-Key`を生成し、二重送信と通信再試行では同じキーを使用する
+- バックエンドは署名付きCookieによる認証とCSRF対策を検証する（NFR-SEC-007）
 - バックエンドはSessionが認証済みマーケターに属する有効な親Sessionであることを検証する
 - アーカイブ済み親Sessionでは新しい処理を開始しないが、アーカイブ前に完了した同一Requestの冪等Responseは認証と所有権を確認して再返却できる
 - バックエンドは安全に解析・マスクした最終Requestを信頼済み承認イベントとして保存し、その後のSchema検証結果も同じTurnへ保存する
@@ -123,12 +124,12 @@
 - 承認後はLLMを呼ばず、UIからアプリケーションAPIを呼び出す
 - 施策案では施策upsert APIを実行する
 - 投稿案では同じ承認操作を投稿内容の確定とX投稿APIの実行指示として扱う
-- 会社所有権、権限、入力Schemaのいずれかを検証できない場合は実行を拒否する
+- 会社所有権、入力Schemaのいずれかを検証できない場合は実行を拒否する。別会社の対象は、存在しない対象と区別せず「存在しない」として扱う
 - 認証と親Session所有権を検証できた後のAPIエラーは、構造化した結果として指定親SessionのAPI実行Turnへ保存する
 - 保存したAPIエラーは次回の親Agent TurnのContextへ含め、Agentワークフローから観測可能にする
 - APIエラーの保存だけでは親Agentを自動起動しない
 - APIエラーを観測したAgentは、ユーザーの新しい承認なしに施策保存やX投稿を再実行しない
-- 認証または親Session所有権を検証できないエラーは、安全な保存先を確定できないためそのRequestのAgent履歴へ保存しない
+- 認証、CSRF検証、または親Session所有権を検証できないエラーは、安全な保存先を確定できないためそのRequestのAgent履歴へ保存しない
 - `AGENT_SESSION_NOT_FOUND`では、UIが利用可能な親Sessionを選択または作成し、マスク済みエラーを新しいTurnへ送信して親Agentワークフローへ接続する
 - バックエンドは施策upsertとX投稿を永続的な冪等性レコードで保護し、同じ承認操作による業務データとAgent履歴の重複作成を防止する
 - 完了済みの同一Requestが再送された場合は、冪等性レコードに保存した確定HTTP StatusとResponse Bodyを返す。`outcome_unknown`は手動照合まで暫定Responseを返し、解決後は確定Responseを返す
@@ -148,6 +149,8 @@
 
 施策upsert APIは`POST /api/v1/agent-sessions/{session_id}/campaigns`とし、Request Bodyの`id`が省略または`null`なら新規作成、値があれば同じ会社の既存施策を上書きする。
 
+既存施策の変更案では、提案時点の施策の`updated_at`をフォームに保持し、承認時にRequest Bodyの`expected_updated_at`として送信する。DBの`updated_at`と一致しない場合は、別のSessionまたは別のマーケターによる更新を上書きしないよう、上書きを拒否する。
+
 施策と検索用Embeddingの保存、成功API Result、Turn完了、およびAPI冪等性レコードの成功確定は同一Transactionで行う。同じ`Idempotency-Key`の同一Requestを再送しても、新しい施策またはAgent Turnを作成しない。
 
 ### 管理対象例
@@ -157,7 +160,6 @@
 - 施策を実施する背景
 - 施策の目的
 - 施策内容
-- 施策ステータス
 
 ---
 
@@ -208,6 +210,8 @@
 X投稿APIはX API呼び出し前に永続的な冪等性レコードで実行権を確定する。同じ`Idempotency-Key`の同一Requestを再送してもXへ再投稿せず、X投稿結果が不明な場合も自動再投稿しない。
 
 X APIへの送信開始を永続化してから投稿し、送信開始後に結果不明となったRequestは手動照合まで`outcome_unknown`として保持する。X公開成功を確認した場合だけ、管理された復旧TransactionでPost関連データを作成して成功状態へ変更する。
+
+X投稿の成功を確認した直後に、DB保存より先にX投稿結果（X投稿ID、送信本文、UTM付きURL、公開日時）を永続化する。その後のDB保存だけが失敗した場合は、同じ`Idempotency-Key`の再送でDB保存だけを再実行し、Xへは再投稿しない。
 
 ## 機能
 
@@ -687,7 +691,7 @@
 
 永久的なエラーについては無限リトライしない。
 
-X投稿は非冪等な外部作用として扱い、X APIへRequestを送信した後のタイムアウト、通信切断、結果不明エラーでは自動再試行しない。自動再試行できるのはX APIへの送信前、またはXが投稿未作成を明確に返した場合に限る。
+X投稿は非冪等な外部作用として扱い、X APIへRequestを送信した後のタイムアウト、通信切断、結果不明エラーでは自動再試行しない。自動再試行できるのはX APIへの送信前、またはXが投稿未作成を明確に返した場合に限る。X投稿の成功後にDB保存だけが失敗した場合は、Xへ再投稿せず、保存済みのX投稿結果を使ってDB保存だけを再実行する。
 
 ---
 
@@ -754,7 +758,12 @@
 - Responseの再返却では業務データとAgent履歴を追加しない
 - 施策または投稿の保存、成功API Result、Turn完了、冪等性の成功確定を同一Transactionで行う
 - X投稿の結果を確定できない場合は`outcome_unknown`として保存し、自動再投稿しない
-- X投稿では外部作用開始前後を永続的に区別し、開始前の期限切れ処理だけを安全に再開する
+- X投稿では外部作用開始前後を永続的に区別し、開始前の期限切れ処理は安全に再開する
+- X投稿の成功を確認した直後に、DB保存より先にX投稿結果を永続化する
+- X投稿結果を永続化した後の期限切れ処理またはDB保存失敗では、Xへ再投稿せず、保存済みの結果でDB保存だけを再開する
+- X投稿結果を永続化できなかった場合と、永続化前に処理が中断した場合は`outcome_unknown`として手動照合へ送る
+- 再送されないまま残った、X投稿結果の保存済みで未完了のRequestを、運用で検出できる
+- 施策の上書きは、提案時点の`updated_at`と一致する場合だけ実行し、一致しない場合は`409`で拒否する
 - 同じ投稿内容の未解決Requestが別キーに存在する場合も、新しいX投稿を拒否する
 
 ---
@@ -978,6 +987,30 @@
 - Prompt Injectionとして検出・記録する
 - X投稿を実行しない
 
+---
+
+## NFR-SEC-007 認証とセッションを保護する
+
+利用者の認証と、認証済みセッションからの状態変更を保護する。
+
+- ユーザーは事前登録とし、登録機能を設けない。認証はメールアドレスとパスワードで行う
+- パスワードは平文で保存せず、argon2でハッシュ化した値だけを保存する
+- 認証は、サーバー側に状態を持たない署名付きCookie（`HttpOnly`、`Secure`、`SameSite=Lax`）で行う
+- 有効期限は、アイドル8時間と絶対上限7日とする
+- 状態変更APIには、`Origin`検証と署名付きダブルサブミットトークンの両方を適用する
+- ログイン失敗時は、メールアドレスの登録有無を判別できない応答を返す
+- 別会社または別マーケターの対象は、存在しない対象と区別せず`404`として扱う
+- パスワード、Cookie、CSRFトークンをログへ出力しない
+
+MVPでは、Cookieのサーバー側での個別失効は行わない。ログアウトはブラウザのCookie削除だけとし、盗まれたCookieは有効期限まで有効である。
+
+### 受入条件
+
+- 署名が不正、または期限切れのCookieでは、認証が必要なAPIが`401`を返す
+- `Origin`が許可リストと一致しない、または`X-CSRF-Token`が不正な状態変更Requestは`403`で拒否される
+- 存在しないメールアドレスと誤ったパスワードで、同じ応答が返る
+- 別会社のCampaign IDを指定したRequestが、存在しないIDと同じ`404`を返す
+
 --- 
 
 # 9. アイデア・独創性に関する設計方針
--- a/docs/v.0.1/DATABASE.dbml
+++ b/docs/v.0.1/DATABASE.dbml
@@ -12,7 +12,7 @@
     - 施策に紐づく投稿は同じ会社に所属しなければならない。
     - 業務データを操作するマーケターは、そのデータを所有する会社に所属しなければならない。
     - 記憶と、それに紐づく施策または投稿は同じ会社に所属しなければならない。
-    - 記憶、施策検索、投稿検索用の埋め込みベクトルは、単一設定されたモデルと次元数を使用して生成する。
+    - 記憶、施策検索、投稿検索用の埋め込みベクトルは、単一設定されたモデルと次元数を使用して生成する。MVPはopenai/text-embedding-3-small（OrcaRouter経由）、1536次元とし、各埋め込み列はvector(1536)、コサイン距離のHNSWインデックス（vector_cosine_ops）をSQL migrationで追加する。
     - 施策と施策検索用の埋め込みベクトルは同じトランザクションで保存・更新し、埋め込み生成に失敗した場合は施策を保存しない。
     - 投稿検索用の埋め込みベクトルはXへの投稿前に生成し、X投稿成功後に投稿と同じトランザクションで保存する。埋め込み生成に失敗した場合はXへ投稿しない。
     - postsにはX公開成功を確認できた投稿だけを保存する。各Postは成功状態へ確定したpublish_x_postのAPI冪等性Requestを一意に参照し、失敗・処理中・結果不明のRequestをget_postとsearch_postsの取得元にしない。
@@ -42,31 +42,12 @@
   '''
 }
 
-Enum user_status {
-  active
-  disabled
-}
-
-Enum campaign_status {
-  draft
-  pending_approval
-  approved
-  rejected
-  archived
-}
-
 Enum post_metric_status {
   pending
   completed
   failed
 }
 
-Enum post_metric_failure_source {
-  x
-  ga4
-  system
-}
-
 Enum agent_type {
   parent
   campaign_planner
@@ -115,7 +96,6 @@
 Enum tool_execution_status {
   pending
   running
-  waiting_for_approval
   completed
   failed
   cancelled
@@ -213,7 +193,7 @@
 
 Table campaign_embeddings {
   campaign_id bigint [pk, note: '【施策ID】']
-  embedding vector [not null, note: '【施策埋め込みベクトル】ターゲット像、実施背景、施策目的、施策内容を正規化したテキストから生成する。単一の埋め込みモデルと次元数を決定後、vector(n)とHNSWコサイン距離インデックスを設定する。']
+  embedding vector(1536) [not null, note: '【施策埋め込みベクトル】ターゲット像、実施背景、施策目的、施策内容を正規化したテキストから生成する。HNSWコサイン距離インデックス（vector_cosine_ops）をSQL migrationで追加する。']
   content_hash varchar(64) [not null, note: '【埋め込み元ハッシュ】正規化した埋め込み元テキストのSHA-256。内容が変わっていない場合の再生成を防ぐ。']
   created_at timestamptz [not null, default: `now()`, note: '【作成日時】']
   updated_at timestamptz [not null, default: `now()`, note: '【更新日時】']
@@ -253,7 +233,7 @@
 
 Table post_embeddings {
   post_id bigint [pk, note: '【投稿ID】']
-  embedding vector [not null, note: '【投稿埋め込みベクトル】投稿本文からURLを除去して正規化したテキストから生成する。単一の埋め込みモデルと次元数を決定後、vector(n)とHNSWコサイン距離インデックスを設定する。']
+  embedding vector(1536) [not null, note: '【投稿埋め込みベクトル】投稿本文からURLを除去して正規化したテキストから生成する。HNSWコサイン距離インデックス（vector_cosine_ops）をSQL migrationで追加する。']
   content_hash varchar(64) [not null, note: '【埋め込み元ハッシュ】URLを除去して正規化した投稿本文のSHA-256。']
   created_at timestamptz [not null, default: `now()`, note: '【作成日時】']
 
@@ -273,13 +253,13 @@
   utm_source varchar(255) [not null, default: 'x', note: '【UTM流入元】現状x固定']
   utm_medium varchar(255) [not null, default: 'social', note: '【UTMメディア】現状social固定']
   utm_campaign varchar(255) [not null, note: '【UTMキャンペーン】campaignの一意の識別子']
-  utm_content varchar(255) [not null, note: '【UTMコンテンツ】postの一意の識別子']
+  utm_content varchar(255) [not null, note: '【UTMコンテンツ】publish_x_postのIdempotency-Key（UUID）。UTM付きURLはX投稿前に確定する必要があり、その時点ではpost_idが存在しないため、承認操作ごとの一意なキーを使用する。']
   tracked_url text [not null, note: '【トラッキングURL】UTMパラメータを付与した遷移先URL。']
   created_at timestamptz [not null, default: `now()`, note: '【作成日時】']
   updated_at timestamptz [not null, default: `now()`, note: '【更新日時】']
 
   indexes {
-    (utm_source, utm_medium, utm_campaign, utm_content)
+    (utm_source, utm_medium, utm_campaign, utm_content) [unique]
   }
 
   Note: 'UTM生成は決定論的なアプリケーションロジックとし、LLMを呼び出さない。'
@@ -309,7 +289,7 @@
   id bigint [pk, increment, note: '【エージェント記憶ID】']
   company_id bigint [not null, note: '【会社ID】']
   content text [not null, note: '【記憶内容】']
-  embedding vector [not null, note: '【埋め込みベクトル】記憶内容から生成する。単一の埋め込みモデルと次元数を決定後、vector(n)とHNSWコサイン距離インデックスを設定する。']
+  embedding vector(1536) [not null, note: '【埋め込みベクトル】記憶内容から生成する。HNSWコサイン距離インデックス（vector_cosine_ops）をSQL migrationで追加する。']
 
   indexes {
     company_id
